# Copy to .env (gitignored) and fill in. NEVER commit real values — this repo is PUBLIC.
# ROTATE the carrier key in the portal before first use of this build (see README SECURITY).
VITELITY_LOGIN=
VITELITY_PASS=
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-5
DISPATCH_SMS_TO=
PORT=3000
