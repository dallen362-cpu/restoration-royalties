# 🦴 THE BONEYARD — parked concepts to pull from later

**Rule of the road:** the live offer leads with **two simple framings only**. Everything below is built, alive, and good — but intentionally *not* the lead. Pull pieces forward as we grow.

---

## ✅ THE TWO LEAD FRAMINGS (keep front-and-center)

1. **Pitch & Catch + Free Financing** — `cowork-campaign/activate.html`
   - PITCH (front end): free 10-pack local DIDs flood the phone with calls.
   - CATCH (back end): AI Second Brain (cloned from their site) catches every call.
   - Offer: **activate FREE now → keep 100% of your sales (unlimited income) → pay the measly $22.95/line later.** Free factoring/funding/financing for the business break-out.

2. **Missed-Call Rush → Google Calendar Booking (the upsell)** — `activate.html` + `platform.html`
   - The free lines create an inbound rush one cell can't handle.
   - We **show them the calls they missed** (lost jobs), then the paid back end captures 100% and **books them into their Google Calendar.**
   - Front end = free, we get paid on activation; back end = the recurring upsell.

---

## 🦴 BONEYARD — parked, available to pull forward

### Alternate framings (still live in the deck, just not the lead)
- **The self-distributing flywheel** — every subscriber is also a retail/wholesale/white-label distributor (`present.html`).
- **Macro → Micro / Front-End ⇄ Back-End stack** — full two-product identity (`platform.html`).
- **Investor economics & moat** — $229.50 MRC unit, owns DIDs + source code (`present.html#investor`).
- **"One saved call = $7,500" ROI / 32× benchmark** — kept in deck; strong but secondary to the financing angle.
- **SW/South Florida fire urgency** micro case study (`present.html`, smoke page).

### Modules built and live (secondary entry points)
- **White-Label Studio** — URL → `<company>.telvergence.com` branded site + 10 DIDs + cloned Second Brain (`studio.html`).
- **Restoration Royalty white-label deck** (`restorationroyalty/`).
- **WIN TV movie** + channel (real fire footage) (`vault-7kq9x2m4/win-tv.html`).
- **Talking navigation map** (`nav-map.html`).
- **Agent Partner Portal (10% of gross)** + **National Grid Saturation / Authorized Dedicated Provider** (`vault-7kq9x2m4/partner.html`, `grid.html`).
- **Live Storm Dispatch app** + **Smoke campaign w/ lead tracking** (`vault-7kq9x2m4/`).
- **3D talking flagship** (`telvergence-3d.html`).
- **e-signed Letter of Authorization / VoIP agreement** (`agreement.html`).

### Back-end / infra (wire as we go)
- **BOSS CRM/LMS** schema + **ViciDial** client (`backend/`).
- **Dashboard metrics data chain:** **start with the Vitelity API** (call detail records, texts/SMS, and **voicemails captured per DID**) → then **overlay ViciDial's API** (answered/missed, dispositions, booked, agent KPIs) → render on our live dashboard (`vault-7kq9x2m4/watch.html`). Per-DID voicemail capture is the proof: every missed call leaves a voicemail we surface, then convert. This is the data behind the "calls you missed → calls we booked & scheduled" story shown to prospects.
- **Vitelity** provisioning worker + local-presence dialing playbook (954/754 · 305/786 · CNAM).
- **Stripe** live link/keys swap.
- **Platform plans** $499 / $999 / $2,499 + $1,499 setup (`platform.html`).
- **Perplexity research** syntheses (`backend/research/`).

### Domains (DNS pending)
- `demo.telvergence.com` → Telvergence deck (CNAME staged).
- `restorationroyalty.telvergence.com` → RR white-label (needs its own host).
- Live **telvergence.com preserved/untouched** — activatable anytime.

---
*Keep the offer simple. The boneyard is the reserve — pull a bone when the moment calls for it.*
