# HANDOFF — Cowork (Excaliber) → QB Claude Code — Half a Cow FINALIZE — 2026-09-02 (closing ledger)
*(Verbatim closing ledger from David's Cowork session, filed in the build shop by the QB. The Drive copy is the canonical: `Claude/05_Handoffs-and-Ingests/HANDOFF_COWORK_TO_QB_HALFACOW-FINALIZE_2026-09-02.md`. Passcode material intentionally absent.)*

**Agreement (Step 0):** confirmed on all five points — hosts/canonical, division of labor, the two-license price model, the AI-Master-Brain/Telvergence business brain, and the open infra items. One caution on record: the "$25k resold truck licenses + referral royalties" framing raises the FTC franchise/business-opportunity disclosure bar; the illustrative / not-an-offer / no-earnings / FDD rails are on every page, and a franchise lawyer should shape any real offer.

**What shipped tonight** (all additive, `private/` never overwritten, nothing outside `halfacow/`):

| Repo | Commits |
|---|---|
| telvergence | `1e8a799` price-model sync (from RR `e2fe505`) · `254842e` refined private deck + passcode rotated + public pitch (RR `7e47061`) · `4ced449` Chef Stefano 3-Minute Show (RR `ad8a15a`) |
| restoration-royalties | `3f8e362` brand-kit canonical · `7e47061` public two-license pitch |

**Verified live (by content):** public pitch = exclusive-city ×4, zero old labels, 17 slides · private deck gated, gate.json rotated (dated today), deck text absent from served HTML, noindex, unlinked · Stefano show "face of Half a Cow" ×2 · homepage Kitchen Sink ×2, realm/members/showroom all intact. Calculator math checks: 5×2 → HQ $1,250,000, city-owner $50,000, recurring $29,940/yr, 3-yr $1,339,820; full-30 → $7,500,000 up-front · $179,640/yr. (All illustrative.)

**Passcode:** rotated; the new 4-digit code lives only on Excaliber (`Downloads\halfacow-pitch\PRIVATE-PASSCODE.txt`) — never in chat, repo, or the brain.

**Tier-3 backup (F:):** the three GitHub restore-point zips are on F: — RR release `ebf1c6f` (10.2 MB), RR restore `8f31b6b` (10 MB), telvergence restore `7271cca` (391 KB) — in `F:\AI-Master-Brain\Telvergence\2026-09-02\github-restore-points\`.

**Two honest open items** (nothing lost, both awaiting David's word):
1. Chef Stefano PITCH editions (`pitch/stefano/` public + `private/stefano/` gated, same passcode) — requested but not built this session; Cowork stood down when the ledger was called for. His 3-Minute Show is already live. Cowork builds them on go, or the QB builds the public one and Cowork syncs it — same pattern as the presentation.
2. Sub-brain zip to F: the code restore points are done; the Drive sub-brain markdown copy to F: is still pending (tiny; already mirrored in Drive). Finishable in minutes.

**Not started, by design:** Cloudflare Access on `/halfacow/private/` and any site-wide gate — those wait for a David-present infra session.

Everything assigned this session is done and verified; the two items above are queued, not dropped. Cowork stopped here.

---
**QB note (2026-09-02, after ingest):** the QB is building the full custom-branded edition tree `halfacow/stefano/` (gold master + hub + inventory + Cow Connect + Platinum + 5 storefronts + its own show) in the build shop; when merged, Cowork syncs `halfacow/` once more (`/XD private`) so `telvergence.com/halfacow/stefano/` matches.
